package com.eazpire.wear.discovery

/** AR feature modes selected via the Eazy mascot menu. */
enum class ArtifactArFeatureMode {
    /** Minimal UI — virtual light and close only. */
    Viewer,
    /** Default loot placement (hand tap). */
    Hand,
    /** Paint on detected surfaces, save with cloud anchor. */
    Canvas,
}
